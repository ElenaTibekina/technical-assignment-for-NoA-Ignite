import { colors, fonts } from '../consts/styleVariables';

export const cardInfoTheme = {
  cardBackgroundColor: colors.colorWhite,
  cardTextColor: colors.colorBlack,
  textSize: fonts.defaultTextSize
}

export const cardErrorTheme = {
  cardBackgroundColor: colors.colorAlertBackground,
  cardTextColor: colors.colorDanger,
  textSize: fonts.errorTextSize
}